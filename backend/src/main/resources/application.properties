# Server configuration
server.port=8080

# Database configuration
# H2 configuration (commented out)
# spring.datasource.url=jdbc:h2:mem:doctorapp
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
# spring.h2.console.enabled=true
# spring.h2.console.path=/h2-console

# PostgreSQL configuration
# H2 Database configuration
# spring.datasource.url=jdbc:h2:mem:testdb
# spring.datasource.username=sa
# spring.datasource.password=
# spring.datasource.driver-class-name=org.h2.Driver
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
# spring.jpa.hibernate.ddl-auto=create-drop
# spring.jpa.show-sql=true
# spring.h2.console.enabled=true

# Server port
server.port=8080
spring.jpa.show-sql=true
# Disable repository initialization to bypass the problematic repository methods
spring.data.jpa.repositories.bootstrap-mode=lazy

# JPA/Hibernate properties

spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.format_sql=true

# Actuator exposure
management.endpoints.web.exposure.include=health,info,mappings
management.endpoint.health.show-details=always

For production with PostgreSQL (commented out for development)
spring.datasource.url=jdbc:postgresql://localhost:5432/doctor-app
spring.datasource.username=postgres
spring.datasource.password=Haja$2431
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Logging
logging.level.org.springframework=INFO
logging.level.com.doctorapp=DEBUG